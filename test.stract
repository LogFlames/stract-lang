Person = (name : string, age:u16, height: f32);

/* NOTE(Elias): Should empty parenthesis be treated as a structTemplate? It would work well for functions (that take structTemplate and then scope) */
main = () {
	person = ("David Malte Elias //Lundell", 18, 182) (name: string, age: number, height: number);
	person = ("David Malte Elias Lundell", 18, 182) Person;
};




